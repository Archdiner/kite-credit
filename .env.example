# --- Solana ---
# Leave blank to use public Solana mainnet RPC (rate limited)
# Recommended: Use a dedicated RPC provider like Helius, QuickNode, or Triton
SOLANA_RPC_URL=

# --- GitHub OAuth ---
# Create at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/github/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# --- Google Gemini ---
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# --- Reclaim Protocol ---
# Get from: https://dev.reclaimprotocol.org/
RECLAIM_APP_ID=your_reclaim_app_id
RECLAIM_APP_SECRET=your_reclaim_app_secret

# --- Plaid ---
# Get from: https://dashboard.plaid.com/developers/keys
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_ENV=sandbox

# --- App ---
# Used for OAuth redirects and attestation URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- ZK Attestation ---
# Secret key used for HMAC-SHA256 attestation signing
# Generate with: openssl rand -hex 32
ATTESTATION_SECRET=your_attestation_secret_change_in_production

# --- Supabase ---
# Get from: https://supabase.com/dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key_here
SUPABASE_PRIVATE_KEY=sb_secret_your_key_here

# Also available without NEXT_PUBLIC_ prefix (for server-only use):
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key_here

# --- Encryption ---
# Used to encrypt stored tokens (Plaid, GitHub) in the database
# Generate with: openssl rand -hex 32
DB_ENCRYPTION_KEY=your_db_encryption_key
