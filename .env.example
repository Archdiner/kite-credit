# Kite Credit Environment Variables
# Copy this file to .env.local and fill in the values.
# .env.local is gitignored and will never be committed.

# --- Solana ---
# QuickNode RPC endpoint for mainnet-beta
# Server-side: used by the analysis API
SOLANA_RPC_URL=https://your-quicknode-endpoint.quiknode.pro/your-key/
# Client-side: used by wallet adapter (can be the same URL)
NEXT_PUBLIC_SOLANA_RPC_URL=https://your-quicknode-endpoint.quiknode.pro/your-key/

# --- GitHub OAuth ---
# Create at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/github/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# --- Gemini ---
# Get from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key

# --- Reclaim Protocol ---
# Get from: https://dev.reclaimprotocol.org
RECLAIM_APP_ID=your_reclaim_app_id
RECLAIM_APP_SECRET=your_reclaim_app_secret

# --- App ---
# Used for OAuth redirects and attestation URLs
# --- Plaid (Bank API) ---
# Get from: https://dashboard.plaid.com/team/keys
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_ENV=sandbox # sandbox | development | production

# --- Vercel ---
# Automatically set by Vercel, but good to have for local dev parity
# VERCEL_URL=your-project.vercel.app
